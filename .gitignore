.omo/
node_modules/
# NOTE: no global `dist/` ignore — this is a distribution repo; the published
# plugin under plugins/omo/ ships its built dist/ (the runtime deliverable).
.env
.env.*
*.tgz
.DS_Store
